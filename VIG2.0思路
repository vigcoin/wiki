1、提供地址表，所有有余额的地址都包含进去
2、每个地址表都对应一个域名功能，方便记忆与未来提供服务
3、交易不在打包成区块，每个交易检验成功即入池，无打包费用，每个节点可自行限制每地址交易次数与最低金额。全网取平均，进行对交易的限制。数据定期更新。
4、采用节点挖矿模式，一个IP限定挖矿地址数量。出矿平均分配，按10年发完。
5、考虑提供智能合约，但是必须要包含对应的服务。服务通过域名或者地址提供。智能合约的创建不需要花费VIG，但是智能合约创建的代币只能通过使用VIG交易获得。
6、每个节点就是一个服务器，智能合约绑定的服务必须可以通过地址或者域名找到。
